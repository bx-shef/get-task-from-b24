# Обработка события: от задачи клиента до задачи у нас

> Last reviewed: 2026-08-26

**Главный доменный документ.** Что происходит между событием на портале клиента и
созданной задачей в нашем Битрикс24: критерии отбора, перенос полей, очередь,
уведомление в Telegram, ошибки.

> ⬜ Помеченное так — не решено, ждёт ответа владельца. Открытые вопросы собраны в конце.

## Общая схема

```
Портал клиента (локальное приложение «только API»)
        │  ONTASKADD (POST, в теле только ID задачи)
        ▼
  наш обработчик  ──▶ ответ 200 сразу, до всякой работы
        │                (события Б24 НЕ ретраятся: медленный или упавший
        │                 ответ = задача потеряна молча)
        ▼
     ОЧЕРЕДЬ
        │  1. tasks.task.get на портале клиента — вычитать поля
        │  2. проверить критерии
        │  3. дедуп по журналу (портал + ID задачи)
        │  4. tasks.task.add в нашем Б24 (входящий вебхук)
        │  5. сообщение в Telegram
        ▼
   Наш Битрикс24
```

⚠ **Обработчик не делает работу — он её ставит в очередь.** В событии приходит только
`ID` (см. [`B24_EVENTS.md`](B24_EVENTS.md)), значит каждая обработка — минимум два
похода по сети (чтение у клиента + запись у нас) плюс Telegram. Делать их в
синхронном ответе нельзя: любой таймаут у клиента или у нас превращается в
безвозвратно потерянную задачу.

## Критерии отбора

Задача переносится, только если выполнены **оба** условия:

1. **Название начинается с `#support`.**
2. **Исполнитель задачи** (`responsibleId`) равен «особому» id, указанному для этого
   портала в переменных окружения рядом с доменом.

Не прошло хотя бы одно — событие считается обработанным и **успешно** отбрасывается
(это норма, а не ошибка: на портале клиента задачи создаются постоянно).

⚠ **Портал, которого нет в списке поддерживаемых, — тоже отказ**, даже если событие
подписано и подлинно. Список из окружения — это и есть перечень тех, кого мы
обслуживаем.

**Префикс сравнивается без учёта регистра** (`#support`, `#Support`, `#SUPPORT` — все
проходят), ведущие пробелы перед `#` игнорируются. **В названии задачи у нас префикс
срезается** вместе с пробелом после него: заголовок читается чище, а признак «это
поддержка» и так виден по тому, куда задача приехала.

⚠ Срезка не должна давать пустое название: задача, названная ровно `#support`, обязана
получить осмысленный заголовок, а не пустую строку (`tasks.task.add` требует `title`).

⚠ **`responsibleId` — id на портале КЛИЕНТА.** У нас на портале такого пользователя
нет и быть не должно; для сопоставления он не годится ни в каком виде.

## Что переносим

| Из задачи клиента | Куда у нас | Примечание |
|---|---|---|
| название | `title` задачи | префикс `#support` срезается |
| содержание | `description` | |
| кто поставил (постановщик) | **в текст `description`** | ⚠ не в `creatorId` — см. ниже |
| крайний срок | `DEADLINE` | нет у клиента → +24 ч от создания (`DEFAULT_DEADLINE_HOURS`) |
| какой Б24 (домен клиента) | **в текст `description`** | ⬜ или отдельное поле/тег |
| ссылка на задачу у клиента | **в текст `description`** | `https://<домен>/company/personal/user/<id>/tasks/task/view/<taskId>/` |

⚠ **«Кто поставил» нельзя положить в `creatorId`.** Это идентификатор сотрудника
**нашего** портала; id постановщика с портала клиента там означал бы другого человека
или ошибку метода. Имя постановщика переносим текстом. Тот же довод касается
исполнителя: у нас задачу создаёт вебхук, и `responsibleId` — наш сотрудник.
Исполнитель у нас — **один id из окружения** (`B24_TARGET_RESPONSIBLE_ID`), общий для
всех порталов.

⚠ **Домен клиента и ссылка обязаны быть в задаче.** Порталов 20–30; задача «сделай
поддержку» без указания, чья она, бесполезна ровно настолько, насколько трудно это
потом восстановить.

## Дедупликация

Журнал: `домен (или member_id) клиента + ID задачи-источника → ID задачи у нас`.
Проверяется **до** создания; повторная доставка события возвращает уже созданный id и
второй задачи не заводит. Без журнала дубли заметит человек, а не тест.

Журнал живёт в Postgres (таблица `transfers`); дедуп держится на первичном ключе базы,
а не на проверке перед вставкой — иначе гонка двух воркеров даёт дубль.

⚠ **Отказ по критериям в журнал НЕ пишется**, иначе журнал переносов перестаёт быть
журналом переносов: на портале клиента задачи создаются постоянно, и 99% из них нас не
касаются. Отказы видны в логе строкой `skip`.

⚠ **Провал ДО проверки критериев** (портал не отвечает) следа в журнале не оставляет —
задача ещё не занята. Видно его в логе (`failed`) и в Telegram на последней попытке.

## Уведомление в Telegram

После успешного создания задачи — сообщение «задача создана, иди делай»: название,
клиент, ссылка на задачу **у нас** и ссылка на задачу **у клиента**.

⚠ **Telegram — отдельный шаг, а не часть создания.** Упавший Telegram не должен
приводить к повторному созданию задачи: сообщение ретраится само по себе, задача — нет.

Адресат один (`TELEGRAM_CHAT_ID`) — и для успешных переносов, и для ошибок.
⚠ Сообщение об ошибке обязано называть портал и ID задачи у клиента: без них оно
сообщает о беде, но не о том, где её искать.

## Ошибки

| Что случилось | Что делаем |
|---|---|
| критерии не прошли | успешный отказ, ничего не создаём; в лог строкой `skip`, **в журнал переносов не пишем** |
| портал не в списке | то же, событию отвечаем 200 |
| очередь недоступна | отвечаем **200** (на череду 500-х портал отключает обработчик совсем) и оставляем в журнале строку `failed` — по ней задачу заводят руками |
| `tasks.task.get` у клиента упал | ретрай с задержкой; исчерпали — сигнал оператору |
| наш Б24 недоступен | ретрай; задача уже принята, терять её нельзя |
| задача уже переносилась | ничего не делаем, это норма |
| Telegram упал | ретрай только сообщения |

⚠ **«Событие принято» и «задача создана» — разные состояния**, и второе может не
наступить часами. Значит нужно место, где видно застрявшие события, иначе про потерю
узнаёт клиент.

## Персональные данные

Перенос по своей природе выносит данные за контур клиента, и это надо называть вслух.

| Что | Откуда | Куда |
|---|---|---|
| название и содержание задачи | портал клиента | наш Битрикс24 (название — ещё и в Telegram) |
| ФИО постановщика | `user.get` на портале клиента | текст описания задачи у нас |
| домен портала, id задачи | событие | наш Битрикс24, Telegram, логи |

⚠ **В логи и телеметрию не попадает ничего из первых двух строк** — только домен,
идентификаторы и код исхода. Проверяется чтением `log()`: он принимает готовый объект,
и класть туда содержимое задачи надо специально.

⚠ Текст с чужого портала обрезается по длине (250 символов на название, 20 000 на
описание) перед отправкой: одна задача с мегабайтным описанием иначе превращается в
мегабайтный запрос к `tasks.task.add` и в сообщение, которое Telegram отвергнет целиком.

## Открытые вопросы

**Решено 2026-08-26:**

1. **Домен обработчика — `get-task-from-b24.bx-shef.by`.** Он попадает в форму
   локального приложения у каждого клиента, поэтому менять его потом дорого.
2. **Секреты порталов — в окружении, живые OAuth-токены — в Postgres.** Токены
   продлеваются в рантайме, окружение не меняется (разбор вариантов —
   [`CLIENT_APP.md`](CLIENT_APP.md)).
3. **Префикс `#support`** — без учёта регистра, в названии у нас срезается.

4. **Исполнитель у нас — один id из окружения** (`B24_TARGET_RESPONSIBLE_ID`) на все
   задачи со всех порталов. Меняется строкой в `.env`, без правки кода.
5. **Нет крайнего срока — ставим +24 часа** от момента создания задачи у нас
   (`DEFAULT_DEADLINE_HOURS`, по умолчанию 24).
6. **Telegram — один `chat_id`** и для «задача создана», и для ошибок переноса:
   застрявший перенос — это тоже «задача не приехала», и узнать о нём надо сразу.

**Ещё открыто:**

7. Группа/проект создаваемой задачи у нас (пока — без группы).
8. Нужны ли `ONTASKUPDATE`/`ONTASKDELETE` (синхронизация после переноса) — сейчас
   вынесены в [`IDEAS.md`](IDEAS.md).
