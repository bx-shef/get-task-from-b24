# События и REST-методы Битрикс24

> Last reviewed: 2026-08-26

Контракт с двумя порталами: что нам **приходит** от портала-источника и что мы
**вызываем** на портале-приёмнике. Факты ниже взяты из официальной документации
(apidocs.bitrix24.ru), а не выведены; помеченное ⬜ — решение постановки, его ещё нет.

## Входящее событие: `ONTASKADD`

Скоуп `task`, подписка — `event.bind`. Приходит **POST**-запросом на наш обработчик.

```json
{
  "event": "ONTASKADD",
  "data": {
    "FIELDS_BEFORE": { "ID": 123 },
    "FIELDS_AFTER":  { "ID": 123 },
    "IS_ACCESSIBLE_BEFORE": "N",
    "IS_ACCESSIBLE_AFTER":  "undefined"
  },
  "ts": "1466439714",
  "auth": { "domain": "…", "member_id": "…", "application_token": "…", "access_token": "…", "…": "…" }
}
```

⚠ **В событии приходит ТОЛЬКО `ID` задачи, а не её поля.** `FIELDS_AFTER` содержит
единственный ключ `ID`; ни названия, ни исполнителя, ни сроков там нет. Значит перенос
физически невозможен без второго шага — вычитать задачу методом `tasks.task.get` на
портале-источнике. Это определяет архитектуру: обработчик события и сборщик данных —
разные шаги, и второй ходит в портал по токену, а не по содержимому запроса.

⚠ **`auth` приходит не всегда.** Если хит, породивший событие, не привязан к конкретному
пользователю портала, токены в событии отсутствуют. Полагаться на `auth.access_token` из
тела нельзя — нужны свои сохранённые токены портала. Содержимое `auth` проверяем в коде
явно.

⚠ **`application_token` — единственное, что отличает событие портала от подделки.**
Наш эндпоинт открыт миру: сверяем токен с сохранённым при установке **до** любой работы.

⚠ **События не ретраятся.** Пропущенное или отвергнутое ошибкой событие потеряно
навсегда — задача просто не приедет, и молча. Отсюда правило: обработчик отвечает
быстро и успешно, а вся работа уходит в очередь/журнал.

⚠ **До завершения установки приложения события не отправляются** — если на портале
установка не финализирована, обработчик не позовут ни разу.

⚠ **`ONAPPUPDATE` обрабатывается наравне с установкой** (`/b24/install`): в нём
приходит **обновлённый `application_token`**. Клиент правит что-нибудь в форме
приложения — и без этой ветки каждое следующее событие получало бы от нас 401 и
терялось, а внешне всё выглядело бы установленным и подписанным.

⚠ **`ONAPPUNINSTALL` обрабатывается на `/b24/handler`** и удаляет токены портала:
иначе удалённое приложение остаётся в базе действующей установкой, и портал числится
подключённым, ничего не перенося.

⬜ **Дозаполняется:** подписываемся ли также на `ONTASKUPDATE` / `ONTASKDELETE`
(см. [`IDEAS.md`](IDEAS.md) — синхронизация после переноса вынесена «на потом»).

## Исходящие методы REST

Учёт ведём здесь: добавили вызов — добавили строку. Иначе список нужных приложению
скоупов собирается по коду вручную и всегда с ошибкой.

| Метод | Скоуп | Портал | Где вызывается | Зачем |
|---|---|---|---|---|
| `event.bind` | `task` | источник | `src/b24/tasks.ts` (`bindTaskAddEvent`) | подписка на `ONTASKADD` при установке |
| `tasks.task.get` | `task` | источник | `src/b24/tasks.ts` (`fetchSourceTask`) | вычитать поля задачи по `ID` из события |
| `user.get` | `user_brief` | источник | `src/b24/tasks.ts` (`fetchUserName`) | имя постановщика в описание; **необязателен** — без него будет «id N». Вызывается с `filter`, ответ сверяется по id: метод возвращает ФИЛЬТРОВАННЫЙ СПИСОК, и непонятый параметр означает «отдать всех» |
| `app.info` | — | источник | `src/b24/tasks.ts` (`verifyPortalToken`) | доказательство подлинности установки: вызов уходит на адрес из реестра |
| `tasks.task.add` | `task` | приёмник | `src/b24/tasks.ts` (`createTargetTask`) | создать задачу-копию |

⚠ **Формат даты — `YYYY-MM-DDThh:mm:ss±hh:mm`**, без миллисекунд и без `Z`.
`toISOString()` даёт и то, и другое: либо метод падает с `ERROR_CORE`, либо `Z`
игнорируется, время читается как серверное и срок уезжает на смещение пояса портала.

⚠ **Портал отдаёт id и даты СТРОКАМИ** («555», «2026-08-27T15:00:00+03:00»). Сравнение
исполнителя с числом из окружения на нетипизированном ответе давало бы вечное «не тот
исполнитель» — то есть тихий отказ переносить вообще всё. Разбор — `parseSourceTask`,
закреплён тестами.

⬜ **Дозаполняется:** проекты/группы, чек-листы и вложения — по мере надобности.

## Аутентификация двух порталов

| Портал | Роль | Способ доступа |
|---|---|---|
| источник (20–30 шт.) | шлёт нам событие, отдаёт поля задачи | локальное приложение «только API», OAuth; токены в БД, шифрованные |
| приёмник (наш) | принимает создание задачи | входящий вебхук, `B24_TARGET_WEBHOOK_URL` |

⚠ Токены и адреса порталов — только в окружении/шифрованном хранилище, никогда в коде,
в тестах и в логах. `member_id` пригоден как идентификатор портала в журналах;
`access_token` и `application_token` — нет.

## Дедупликация

Журнал `домен источника + ID задачи-источника → ID задачи у нас` (таблица `transfers`),
запись — **до** создания. Повторная доставка события не заводит вторую задачу.

⚠ Дедуп держится на первичном ключе базы (`on conflict do nothing`), а не на проверке
«есть ли уже запись» перед вставкой: два события об одной задаче могут обрабатываться
одновременно, и между чтением и записью успевает пролезть второй воркер. Плюс второй
рубеж в очереди — `jobId` вида `домен--id`.

## Ссылки

- [Обзор событий задач](https://apidocs.bitrix24.ru/api-reference/tasks/events-tasks/index.html)
- [`ONTASKADD`](https://apidocs.bitrix24.ru/api-reference/tasks/events-tasks/on-task-add.html)
- [`tasks.task.add`](https://apidocs.bitrix24.ru/api-reference/tasks/tasks-task-add.html)
- [Обработчик событий](https://apidocs.bitrix24.ru/api-reference/events/index.html)
